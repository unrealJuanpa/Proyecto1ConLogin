@model IEnumerable<Proyecto1ConLogin.Models.Tipo_de_show_a_presentar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion_tipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion_tipo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Tipo_show }) |
            @Html.ActionLink("Details", "Details", new { id=item.Tipo_show }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Tipo_show })
        </td>
    </tr>
}

</table>
